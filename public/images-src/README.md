# Put full-resolution SOURCE photography here before running `npm run images`.
#
#   public/images-src/hero/hero.jpg              (hero photograph)
#   public/images-src/products/{slug}.jpg        (product photographs)
#   public/images-src/services/{slug}.jpg        (service/reading photographs)
#
# See IMAGERY.md for the exact prompts and required slugs. The pipeline in
# scripts/generate-images.mjs converts these into optimized AVIF/WebP/JPEG
# derivatives at public/images/ and the UI serves them automatically.
